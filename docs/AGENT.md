# Agent Guidance

Keep portfolio changes focused on the existing Next.js App Router architecture. Prefer existing dependencies and styling conventions. Server-only credentials must remain in environment variables, and user-facing behavior should be validated with the project build and TypeScript check.
